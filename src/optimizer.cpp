#include "interpreter.h"
#include "vector"
#include "tuple"

typedef struct {
	vector<int> effects;
	int move_sum;
} loop;

vector<token> optimizer(vector<token> raw) {
	vector<token> final;

	return final;
}